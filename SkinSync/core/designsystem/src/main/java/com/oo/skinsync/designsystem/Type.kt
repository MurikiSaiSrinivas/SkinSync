package com.oo.skinsync.designsystem

import androidx.compose.material3.Typography

// Material 3 default type scale. Centralized so a brand font can be swapped in
// one place later without touching screens.
val SkinSyncTypography = Typography()
